// Temporarily disabled due to build errors
// This component is not used in the current wizard flow

import React from 'react';

export const DownloadProgress: React.FC<any> = () => {
  return <div>Component temporarily disabled</div>;
};